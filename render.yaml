services:
  - type: web
    name: fullstack-app
    env: docker
    plan: free
    repo: https://github.com/YOUR_USERNAME/YOUR_REPO
    dockerCommand: docker-compose up --build
    envVars:
      - key: PORT
        value: 3000
